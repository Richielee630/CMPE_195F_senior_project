{
  "name": "react-coingecko",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "scripts": {
    "start": "node scripts/dev.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run && node --test server/*.test.mjs",
    "audit": "npm audit",
    "dev:ui": "vite",
    "dev:api": "node --env-file-if-exists=.env --watch server/index.mjs",
    "test:server": "node --test server/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "@vitejs/plugin-react": "^6.1.1",
    "vitest": "^5.0.0",
    "jsdom": "^30.0.1",
    "@testing-library/react": "^16.3.0"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  }
}
